"use client";

export function BackgroundOrbits() {
  // Background orbits have been removed for a cleaner look.
  // Keeping this component to avoid touching page section structure.
  return null;
}
